services:
  broker:
    build: .
    ports:
      - "8080:8080"
    environment:
      - AA_PORT=8080
      - AA_ADMIN_SECRET=change-me-in-production
      - AA_SEED_TOKENS=true
      - AA_LOG_LEVEL=verbose
    networks:
      - agentauth-net

networks:
  agentauth-net:
    driver: bridge

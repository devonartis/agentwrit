# Finding 07: Binary Name `aactl` Needs Rename to `awrit`

**ID:** TD-CLI-001
**Severity:** MEDIUM (mechanical rename, no logic change)
**Status:** UNRESOLVED
**Audit reference:** C5

---

## What Was Found

The CLI binary is named `aactl` (short for "AgentAuth Control") across the codebase:

- Directory: `cmd/aactl/` (10 files)
- Cobra command name: `cmd/aactl/root.go:16` → `Use: "aactl"`
- Help text: `cmd/aactl/root.go:18` → `"aactl is the operator CLI for managing the AgentAuth broker."`
- 24 Go references in `cmd/` alone
- Additional references in: docs (`docs/aactl-reference.md`), scripts (`scripts/*.sh`), CHANGELOG, README, Dockerfile, config file generation (`configfile.go:108`)

**Decision (2026-04-10):** Rename to `awrit`.

## Why This Is a Problem

It's not a bug — it's a naming decision forced by the rebrand. The CLI name carries the old brand. Every time an operator types `awrit`, it should evoke "AgentWrit" — not "AgentAuth."

## Solution

This is a mechanical rename with a large blast radius but zero logic change.

### Step 1: Rename the directory

```bash
mv cmd/aactl cmd/awrit
```

### Step 2: Update Cobra command name

In `cmd/awrit/root.go`:

```go
// Before:
Use:   "aactl",
Long:  "aactl is the operator CLI for managing the AgentAuth broker.",

// After:
Use:   "awrit",
Long:  "awrit is the operator CLI for managing the AgentWrit broker.",
```

### Step 3: Update all internal references

```bash
# Find all remaining references:
grep -rn 'aactl' --include='*.go' --include='*.md' --include='*.sh' --include='*.yml' .
```

Files that need updating:

| File | What to change |
|------|---------------|
| `cmd/awrit/main.go` | No change needed (just calls root) |
| `cmd/awrit/init_cmd.go` | Help text referencing `aactl` |
| `cmd/awrit/init_cmd_test.go` | Test assertions referencing `aactl` |
| `internal/cfg/configfile.go:108` | `"Generated by: aactl init"` → `"Generated by: awrit init"` |
| `docs/aactl-reference.md` | Rename file to `awrit-reference.md`, update all content |
| `scripts/stack_up.sh` | Any `aactl` references |
| `scripts/gates.sh` | Any `aactl` references |
| `CHANGELOG.md` | Historical entries can keep `aactl` (it's history). New entries use `awrit`. |
| `README.md` | All `aactl` → `awrit` |
| `Dockerfile` | Binary name in build stage |
| `Makefile` (if exists) | Binary name |
| `.github/workflows/*.yml` | Any references |

### Step 4: Update docs

- Rename `docs/aactl-reference.md` → `docs/awrit-reference.md`
- Update all inline references
- Add a note: "`awrit` was previously named `aactl`. The binary was renamed as part of the AgentWrit rebrand."

### Step 5: Update the brand constant (if Finding 04 is done)

If `internal/cfg/constants.go` was created with `CLIBinaryName = "awrit"`, use that constant in `configfile.go` instead of the literal.

## Files Changed

~20+ files, but all are mechanical text replacements. Zero logic changes.

## Verification

1. `grep -rn 'aactl' --include='*.go' --include='*.md' --include='*.sh' --include='*.yml' .` → only historical CHANGELOG entries remain
2. `go build ./cmd/awrit` → produces `awrit` binary
3. `./awrit --help` → shows "awrit" in usage, not "aactl"
4. `./awrit init --help` → shows correct help text
5. `go test ./cmd/awrit/...` — all pass
6. `go test ./...` — all pass (no other package references the old binary name)

## Note

This finding should be handled as **PR 2**, separate from the correctness fixes in PR 1. It's noisy (lots of files) but zero-risk (no logic change). Can proceed in parallel with PR 1b-1d.
